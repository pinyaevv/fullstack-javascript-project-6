extends ../layouts/application.pug

block header
  | #{t('views.statuses.index.header')}

block content
  +formFor(status, { action: route('statuses'), method: 'POST' })
    +input(status, 'name', errors)
    +submit(t('views.statuses.index.new'))
